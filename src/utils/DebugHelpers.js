// UTILS ========================================
export function cl(log1, log2) {
  if (arguments.length === 1) {
    return console.log(log1);
  } else {
    return console.log(log1, log2);
  }
}
// ============================================
